{
  admin off
  persist_config off
  auto_https off
}

:{$PORT:3000} {
  encode zstd gzip
  root * /srv

  handle /health {
    respond "ok" 200
  }

  handle {
    try_files {path} /index.html
    file_server
  }
}
